<%= form_with(model: dish, local: true) do |form| %>
  <% if dish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dish.errors.count, "error") %> prohibited this dish from being saved:</h2>

      <ul>
      <% dish.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <%= form.label :restaurant_id %>
      <%= collection_select(:dish, :restaurant_id, Restaurant.all.collect, :id, :name, {}) %>
    </div>


 <div class="field">
   <%= form.label :category_id %>
   <%= collection_select(:dish, :category_id, Category.all.collect, :id, :name, {}) %>
 </div>



  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>




  <div class="field">
    <%= form.label :cooktime %>
    <%= form.time_select :cooktime %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
